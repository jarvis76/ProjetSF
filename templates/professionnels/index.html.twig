{% extends 'base.html.twig' %}

{% block title %}Formulaire {% endblock %}

{% block body %}
    {{ form_start(formulaire) }}
    {{ form_widget(formulaire) }}
    {{ form_end(formulaire) }}
{% endblock %}
